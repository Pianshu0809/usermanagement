import { Lengthlimit } from './lengthlimit';

describe('Lengthlimit', () => {
  it('should create an instance', () => {
    const directive = new Lengthlimit();
    expect(directive).toBeTruthy();
  });
});
